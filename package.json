{
  "name": "halolmia",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "packageManager": "npm@11.16.0",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@turbo/windows-64": "^2.10.3",
    "turbo": "^2.5.8"
  },
  "engines": {
    "node": ">=18"
  }
}
